{
  "name": "cabinet-cenon-server",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "description": "Backend serveur cabinet dentaire Cenon (Phase 1 : proxy SQL + snapshot DB)",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/static": "^9.1.3",
    "better-sqlite3": "^12.9.0",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^24.12.0",
    "tsx": "^4.19.2",
    "typescript": "~5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
